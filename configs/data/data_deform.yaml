transforms:
  train:
    - EnsureChannelFirstd:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]
        channel_dim: "no_channel"

    - EnsureTyped:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]
        dtype: float32

    # === Intensity normalization (nnUNet style) ===
    - ScaleIntensityRanged:
        keys: ["Image"]
        a_min: 0.0
        a_max: 213.0
        b_min: -5.0
        b_max: 5.0
        clip: true

    # === 3D patch extraction ===
    - RandCropByPosNegLabeld:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]
        label_key: "Mask"
        spatial_size: ${input_size}
        pos: 2
        neg: 0
        num_samples: 2
        image_key: "Image"
        image_threshold: 0.0

    # === Spatial augmentations (if enabled, Skel is already covered) ===
    # - RandFlipd:
    #     keys: ["Image", "Mask", "Mask_OOF", "Skel"]
    #     prob: 0.5
    #     spatial_axis: 0
    # ...

    - ToTensord:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]

  val:
    - EnsureChannelFirstd:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]
        channel_dim: "no_channel"

    - EnsureTyped:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]
        dtype: float32

    - ScaleIntensityRanged:
        keys: ["Image"]
        a_min: 0.0
        a_max: 213.0
        b_min: -5.0
        b_max: 5.0
        clip: true

    - ToTensord:
        keys: ["Image", "Mask", "Mask_OOF", "Skel"]

  test:
    - EnsureChannelFirstd:
        keys: ["Image", "Mask_OOF"]
        channel_dim: "no_channel"

    - EnsureTyped:
        keys: ["Image", "Mask_OOF"]
        dtype: float32

    - ScaleIntensityRanged:
        keys: ["Image"]
        a_min: 0.0
        a_max: 213.0
        b_min: -5.0
        b_max: 5.0
        clip: true

    - ToTensord:
        keys: ["Image", "Mask_OOF"]